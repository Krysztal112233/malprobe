# Bake the backend origin into the build (no trailing slash).
# Unset = same-origin requests (dev proxy / reverse proxy handles routing).
# Cross-origin bases require `cors.allow_origins` in malprobe.toml.
# VITE_API_BASE_URL=http://192.168.1.10:8000
